[required]
token = "lorem_ipsum_token_insert_here"
prefix = "!"
mention = true #should the bot react on mention?
shards = 1
sqlite_path = "./db/"
pg_connection_url = "postgres://username:password@hostname/database"

[optional]
#database_name = "" # defaults to rsbot.db if nothing is entered
#youtube_token = ""
imgur_client_id = "my id"
imgur_client_secret = "my secret"
imgur_refresh_token = "my token"
setting = true